<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <PropertyGroup>
    <Description>Packata.OpenDataContract is a Packata extension that supports deserialization of Open Data Contract Specifications (ODCS). It maps schema elements, properties, quality checks, and metadata directly into strongly typed C# models, enabling validation, introspection, and integration into data platform pipelines..</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Packata.Core\Packata.Core.csproj" />
  </ItemGroup>

</Project>
